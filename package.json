{
  "name": "react-native-template-ts-redux",
  "version": "0.0.7",
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "test": "jest",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "app/**/*.{ts,tsx}": "tslint -c tslint.json"
  },
  "jest": {
    "preset": "react-native"
  }
}
